# Este arquivo é necessário apenas em PRODUÇÃO.
# Em desenvolvimento o docker-compose já tem valores padrão embutidos.
#
# Para produção:
#   cp .env.example .env
#   edite o .env com valores seguros
#   docker-compose up --build

DB_USERNAME=postgres
DB_PASSWORD=troque_esta_senha_forte
JWT_SECRET=gere_com_comando_openssl_rand_hex_64
JWT_EXPIRATION=86400000
DDL_AUTO=validate
SEED_ADMIN_SENHA=troque_imediatamente
SEED_DEFAULT_SENHA=troque_imediatamente
